# VITE_API_URL=https://bagifybackend-qeag.onrender.com/api/v1
# Local backend defaults to port 4000 (see backend/.env PORT=4000). Update this before running if your backend uses a different port.
VITE_API_URL=http://localhost:4000/api/v1
